<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <!-- CSS only -->
        <link rel="shortcut icon" type="image/jpg" href="https://i.imgur.com/neJ8RDk.png"/>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
        <link rel="stylesheet" href="/stylesheets/style.css">
        <!-- JavaScript Bundle with Popper -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous" defer></script>
        <title>FADO Management</title>
    </head>
    <body>
        <nav class="navbar navbar-expand-lg bg-dark">
            <div class="container-fluid">
              <a class="navbar-brand text-white" href="/<%= user ? user.Admin ? 'admin' : 'staff' : null %>">Plan It</a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
                <% if(user) {%>
                <li class="nav-link text-white"> <%= user.name %> </li> 
                <% } %>
                <ul class="navbar-nav">
                  <%if (user) { %>
                    <% if(user.Admin) {%>
                        <a class="nav-link dropdown-toggle" style="color: white;" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false"><i class="bi bi-person icons"></i></a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                          <li> <a class="dropdown-item" href="/admin/new">Add Employee</a> </li>
                          <li> <a class="dropdown-item" href="#">Add Schedule</a> </li>
                          <li> <a class="dropdown-item" href="#">View Schedules</a> </li>
                          <li> <a class="dropdown-item" href="/logout">Log Out</a> </li>
                        </ul>
                    <% } else if(!user.Admin) { %>
                        <li class="dropdown-item ps-1"> <a href="/logout" class="btn btn-outline-light">Log Out</a> </li> 
                    <% } %>
                  <% } else if (!user) { %>
                     <i class="bi bi-google googleIcon me-1 nav-link"></i><a class="btn btn-outline-light" href="/auth/google">Log In</a>
                  <% } %>
                </ul>
              </div>
            </div>
        </nav>
        <% if(user) { %>
          <div class="sideBar bg-dark justify-content-center text-center">
            <div class="d-flex flex-row justify-content-center">
              <i class="bi bi-diagram-3 icons pe-1"></i>
              <p class="text-white">Core</p>
            </div>
            <% if (user.Admin) { %>
              <div class="d-flex justify-content-center align-items-center">
              <i class="bi bi-person-plus-fill icons pe-2"></i>
              <a class="sideBarButtons text-wrap anchor d-flex align-items-center" href="/admin/new">Add Employee</a> 
              </div>
              <div class="d-flex justify-content-center align-items-center">
                <i class="bi bi-clock icons pe-2"></i>
                <a class="sideBarButtons text-wrap anchor d-flex align-items-center" href="#">Add Schedule</a> 
              </div>
              <div class="d-flex justify-content-center align-items-center">
                <i class="bi bi-calendar icons pe-2"></i>
                <a class="sideBarButtons text-wrap anchor d-flex align-items-center" href="#">View Schedules</a> 
              </div>
            <% } else if(!user.Admin) { %>
              <div class="d-flex justify-content-center align-items-center">
                <i class="bi bi-calendar icons"></i>
                <a class="sideBarButtons text-wrap anchor d-flex align-items-center" href="#">View My Schedule</a> 
              </div>
            <% } %>
          </div>
        <% } %>



        
      
         